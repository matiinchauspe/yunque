---
name: yun-placeholder
description: A skill whose only top-level heading is a form field from its own template.
---

# <NN> — <Ticket title>

This is `yun-slice-plan`'s real defect, fixed in `8671789`. Its only `#` heading
was a placeholder from inside its own ticket template, so the document's top-level
heading was a form field rather than the skill's identity.

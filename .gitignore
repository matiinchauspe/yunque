# Work material — NOT versioned.
# The workspace versions ONLY the harness, not the projects' code.
/repos/
/.worktrees/
/.yun/

# Editor multi-root workspace — names repos that exist only on this machine.
*.code-workspace

# System noise
.DS_Store
